# life-plan-lab

FIRE・住宅ローン・教育費・老後を統合した生活設計シミュレーター（V2）。

「未来を当てる」ためではなく「未来を整理する」ためのツール。入力して終わりではなく、
**結果を見る → 条件を変える → また結果を見る → 家族で話す** という試行錯誤を支えることを重視しています。

## 開発

```bash
npm install
npm run dev      # 開発サーバ（http://localhost:5173）
npm run build    # 型チェック + 本番ビルド（dist/）
npm test         # ロジック/ストアのテスト（vitest）
```

静的アプリとしてビルドし、将来 `/lifeplan-v2/` などのサブディレクトリに独立ページとして
公開する想定（`vite.config.ts` の `base: './'` で相対パス出力）。

## アーキテクチャ

- `src/schema` … 型・入力スキーマ（`Field<T>{value, source, label, assumptionText}`）・標準値/おすすめ値・正規化（両モードの合流点 `buildFullInput`）・ざっくり診断の質問定義
- `src/engine` … 純粋関数の計算群（年次/教育費/住宅ローン/FIRE/判定）。UIから完全分離。定数は `engine/constants.ts`
- `src/store` … zustand。ステップ遷移・入力状態・自動保存（localStorage）
- `src/features` … 画面（mode-select / input-steps / results / resume）

## 実機確認チェックリスト（ざっくり診断 / STEP2.5）

スマホ実機、またはブラウザの開発者ツールで **幅375px（iPhone相当）** にして確認してください。

- [ ] `npm run dev` で起動できる
- [ ] 375px幅で、各ステップが大きなスクロールなしに見える（説明・選択肢・入力欄が詰まりすぎない）
- [ ] モード選択 → ざっくり診断を開始できる
- [ ] 下部固定ナビの「次へ／戻る」が常に見える（本文と重ならない）
- [ ] 進捗に「○ / 5」と「あと約○分」が出る
- [ ] 各ステップ上部に短い目的説明が出る
- [ ] 「次へ」「戻る」でページ移動できる（最初のページの戻るはモード選択へ）
- [ ] Recommended（おすすめ値）を押すと反映され、状態が分かる
- [ ] Skip（スキップ）を押すと反映され、状態が分かる
- [ ] Help（？）の補助説明が開閉できる
- [ ] 子ども0人のとき「教育方針」が非表示になる
- [ ] 未回答で「次へ」を押しても赤字エラーにならず、案内＋「このまま進む」が出る
- [ ] 最後のページで結果ダッシュボードへ遷移する
- [ ] 結果画面の「条件を変えてみる」から各カテゴリへ戻って再計算できる
- [ ] 結果画面は結論（総合結果・FIRE達成度・今回のポイント・主な節目・条件変更）がまず見える
- [ ] 詳細（資産推移の拡大・教育費・住宅ローン・タイムライン全件）は「詳しく見る」でBottom Sheetが下から開く
- [ ] 資産推移グラフ（Recharts）が横にはみ出さず、横軸が年齢（◯歳）で表示される
- [ ] 通常表示のグラフはコンパクトで、Heroや今回のポイントより主張しすぎない
- [ ] 「グラフを拡大」でBottom Sheet内に大きいグラフ＋ツールチップ（◯歳（西暦）／資産）が出る
- [ ] グラフ上の節目マーカーとタイムラインの節目が一致している
- [ ] 子ども0人・資産枯渇あり/なしなど、極端な条件でもグラフが崩れない
- [ ] Bottom Sheetは×・背景タップ・Escで閉じられ、内部スクロールで完結する
- [ ] 「今回の試算条件を見る」「見直しのヒントを見る」は折りたたみで開閉できる
- [ ] 結果画面に「税制は簡略化しています」の注記が出る（試算条件の中）
- [ ] 入力途中でリロードすると「続きから再開しますか？」が出る（続き／最初から）
